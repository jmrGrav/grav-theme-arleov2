# 1.0.0
## 18/04/2026

1. [](#new)
    * Dark/light mode toggle with `localStorage` persistence and system `prefers-color-scheme` detection
    * Anti-FOUC inline script (CSP nonce-compatible, no `unsafe-inline` required)
    * Sidebar navigation with collapse button (desktop) and slide-in drawer (mobile)
    * Sticky header: logo + search zone aligned to sidebar width
    * GitHub icon link in header controls
    * Syntax highlighting via highlight.js (Tokyo Night Dark / GitHub Light)
    * Responsive mobile layout (390px → 1440px+)
    * Footer with Grav attribution, Privacy Policy link and Service Status indicator
    * CSP-compliant — no external font CDN, no `unsafe-inline`
    * Compatible with `grav-plugin-csp-nonce`
